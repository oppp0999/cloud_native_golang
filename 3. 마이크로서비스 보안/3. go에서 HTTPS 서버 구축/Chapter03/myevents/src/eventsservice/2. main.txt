1.rest에서 설명한대로

25~32. -> 두 개의 반환된 에러 채널을 두 개의 변수로 잡아낸 후에 이 채널들을 처리하고자 Go select문을 사용 (Go에서 select문은 다수의 채널을 기다리고자 현재의 고루틴을 중단시킬 수 있다)

1. 최초반환되는 어떤 채널도 select case 소환
2. 첫번째 case소환 에러가 발견하면 프린트
3. 첫번째에서 에러가 발생하지 않았을 경우 두 번째 case 소환 에러가 있다면 프린트 없다면 함수를 둘 다를 실행시켜 고루틴에서 빠져나가지 않게 보장하는 매커니즘 필요(고루틴은 값이 전달되기 전까지 중단.)


ExtractConfiguration를 이용해 HTTPS엔드포인트에 대한 기본값, 설정값을 얻을 수 있다.
^^^^
그래서 이것을 main에 호출하여 ServeAPI() 함수 호출을 위해 필요한 정보를 얻게 된다.
